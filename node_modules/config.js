/*! config.js - Loads the config.json from the root */

  module.exports = (function() {

    var fd, fs;

    fs = require('fs');

    fd = fs.readFileSync('config.json', {

      encoding: 'utf8'

    });

    return JSON.parse(fd);
  })();


