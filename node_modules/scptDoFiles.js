/**
 * Created by Liam on 28/07/2015.
 */
var fm = require('FileManager');
var dm = require('DataManager');
var path = require('path');

var compicFile = path.basename(fm.config.compicDataFile);
var backupFile = path.normalize(fm.config.folders.backup + compicFile);
var backuppath = path.normalize(fm.config.folders.backup + compicFile);
var compicData = fm.getCompics();
dm.save(compicData, compicFile, backupFile);